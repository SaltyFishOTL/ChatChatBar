interface:
  display_name: "ChatBar Model Request Runtime"
  short_description: "Diagnose model requests, retries, truncation, and streaming"
  default_prompt: "Use $chatbar-model-request-runtime to diagnose or update ChatBar model requests and retry behavior."
